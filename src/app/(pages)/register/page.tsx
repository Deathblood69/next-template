import {Fragment} from "react";
import {RegisterForm} from "@/auth/RegisterForm";

export default function Page() {
    return (
        <Fragment>
            <RegisterForm/>
        </Fragment>
    )
}